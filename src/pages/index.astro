---
import { getCollection } from 'astro:content';
import Base from '@/layouts/Base.astro';
import Hero from '@/components/Hero.astro';
import About from '@/components/About.astro';
import Services from '@/components/Services.astro';
import PortfolioGrid from '@/components/PortfolioGrid.astro';
import Contacts from '@/components/Contacts.astro';
import Button from '@/components/Button.astro';
import SectionHead from '@/components/SectionHead.astro';

const projects = (await getCollection('projects', ({ data }) => !data.draft))
  .sort((a, b) => b.data.order - a.data.order)
  .slice(0, 6);
---

<Base path="/">
  <Hero />
  <About />
  <Services />

  <section
    id="raboty"
    class="bg-[linear-gradient(180deg,transparent,color-mix(in_srgb,var(--color-accent)_4%,transparent))] py-[72px]"
  >
    <div class="container-x">
      <SectionHead
        class="mb-11 max-w-[46em]"
        eyebrow="Портфолио"
        title="Работы в Абакане и Черногорске"
        desc="Каждый объект — отдельный кейс с фото и составом работ. Нажмите, чтобы посмотреть."
      />

      {
        projects.length > 0 ? (
          <PortfolioGrid projects={projects} />
        ) : (
          <p class="text-muted">Кейсы скоро появятся.</p>
        )
      }

      <div class="mt-10">
        <Button href="/portfolio/" variant="secondary">Все работы →</Button>
      </div>
    </div>
  </section>

  <Contacts />
</Base>
